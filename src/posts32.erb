---
layout: page
title: Posts
---
<div class="container mx-auto px-4 py-8">
  <h1 class="text-4xl font-bold text-center mb-8">Wszystkie posty</h1>

  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
    <% collections.posts.resources.each do |post| %>
      <div class="bg-white shadow-lg rounded-lg p-6 hover:shadow-xl transition-shadow duration-300">
        <article class="prose xl:prose-xl text-center">
          <h2 class="text-2xl font-semibold text-blue-600 hover:text-blue-800">
            <a href="<%= post.relative_url %>"><%= post.data[:title] %></a>
          </h2>
          <p class="text-gray-500 text-sm mt-2"><small><%= post.date.strftime("%Y-%m-%d") %></small></p>

          <% author_key = post.data[:author] %>
          <% author = site.data[:users][author_key] if author_key %>

          <% if author %>
            <p class="mt-4 text-gray-700"><strong>Autor:</strong> <%= author[:name] %></p>
            <% if author[:avatar] %>
              <img src="<%= author[:avatar] %>" alt="Zdjęcie autora" class="w-16 h-16 rounded-full mx-auto mt-2">
            <% else %>
              <p class="mt-2 italic text-gray-500">Zdjęcie niedostępne</p>
            <% end %>
          <% else %>
            <p class="mt-4 italic text-gray-500">Autor nieznany</p>
          <% end %>

          <p class="mt-4 text-gray-600"><%= post.content.strip[0..30] %>...</p>

          <a href="<%= post.relative_url %>" class="inline-block mt-4 px-4 py-2 bg-blue-600 text-white font-medium rounded hover:bg-blue-700 transition-colors">
            Czytaj dalej →
          </a>
        </article>
      </div>
    <% end %>
  </div>
</div>